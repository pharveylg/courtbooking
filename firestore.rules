rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all app documents
    // For production, add authentication and restrict writes
    match /app/{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
