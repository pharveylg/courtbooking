{
  "name": "courtbooking-functions",
  "description": "Tournament engine callables -- the one part of this app where writes are server-adjudicated instead of open per-tenant Firestore rules, because bracket advancement can't tolerate two concurrent 'last write wins' updates the way a booking or a sale can.",
  "engines": { "node": "22" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "private": true
}
